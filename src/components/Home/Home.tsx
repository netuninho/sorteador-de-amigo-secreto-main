import Botao from "../Button/Botao";
import Formulario from "../Form/Formulario";

const Home = () => {
    return (
        <>
            <Formulario/>
            <Botao/>
        </>
    )
};

export default Home